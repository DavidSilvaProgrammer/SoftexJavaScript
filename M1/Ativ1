Acesse pelo menos dois sites de sua preferência e os inspecione com o botão direito do mouse na página web.
Com o código fonte aberto você deve: 

Verificar no código algum elemento que utilize JavaScript; 
Marcar alguns elementos do site; 
Explicar como ele se comporta. Exemplo: entrar no site do Google, inspecionar o botão de pesquisa, verificar o código e explicar qual a finalidade do botão. 

Trabalhe esse código em seu IDE, suba ele para sua conta no GitHub e compartilhe o link desse projeto no campo ao lado para que outros desenvolvedores possam analisá-lo. 

************************************************************
RESPOSTA:

Site 1: google.com

Elemento do site inspecionado: botão "Pesquisa Google".

Comportamento: O botão “Pesquisa Google” encaminha o usuário para uma lista com os sites mais relevantes a respeito 
do termo que ele tenha digitado na barra de busca.

Clicando na opção inspecionar do navegador, surge este código JavaScript na opção bubbling de event:

function(k) {
  var m = b.Vm.Tj();
  _.sy(k);
  k = new Map([
    ["ved", _.jc(f)]
  ]);
  b.oa && k.set("uact", "5");
  _.HUd(b.form, k);
  TUd(b, _.oFd(new _.ty, b.oa ? 3 : 12).setQuery(m).build(), f)
}

---------------------------------------------------------

Site 2: youtube.com

Elemento do site inspecionado: botão "Pesquisar com sua voz".

Comportamento: Ao clicar no ícone do microfone que se encontra ao lado da barra de pesquisa do YouTube, 
surge ao usuário a opção de permitir acesso ao microfone de seu dispositivo. Após o usuário conceder permissão, 
aparece um pop-up com o mesmo icone de microfone. A partir disso, o usuário deve manter pressionado esse icone
enquanto transmite uma mensagem verbal ao aparelho para que a pesquisa seja efetuada.

Clicando na opção inspecionar do navegador, surge este código JavaScript na opção bubbling de event:

function(Cc) {
  null == t || t(Cc);
  ca && (null == Ha || Ha(Cc))
}
